<Page
    x:Class="ortc_standup.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ortc_standup"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Button x:Name="StartStopButton" Content="Start" HorizontalAlignment="Left" Margin="80,40,0,0" VerticalAlignment="Top" Width="120" Height="60" FontSize="20" Click="StartStopButton_Click"/>
    <Border HorizontalAlignment="Left" Height="240" Margin="83,147,0,0" VerticalAlignment="Top" Width="320" BorderBrush="{ThemeResource TextBoxBorderThemeBrush}" BorderThickness="2">
      <MediaElement x:Name="LocalVideoMediaElement"/>
    </Border>
    <Border HorizontalAlignment="Left" Height="240" Margin="486,147,0,0" VerticalAlignment="Top" Width="320" BorderBrush="{ThemeResource TextBoxBorderThemeBrush}" BorderThickness="2">
      <MediaElement x:Name="RemoteVideoMediaElement"/>
    </Border>
  </Grid>
</Page>
